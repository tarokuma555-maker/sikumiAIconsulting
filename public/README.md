# public/

静的ファイルを置く場所です。`/ファイル名` でアクセスできます。

## 講師写真

`teacher.png`(または `.jpg` / `.jpeg` / `.webp`)を置くと、
LPの講師紹介セクションに自動で表示されます。
ファイルがない間はプレースホルダー(枠だけ)が表示されるので、
置き忘れても画像リンク切れにはなりません。

- 表示サイズ: 240×240(正方形にトリミングされます)
- 推奨: 480×480 以上の正方形。人物が中央に写っているもの
